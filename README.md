# kaishaneko

Simple responsive marketing website built with plain HTML, CSS, and JavaScript.

## Run locally

Open `index.html` in your browser, or serve the folder with a static server:

```bash
python3 -m http.server 8000
```
