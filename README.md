# kaishaneko

Simple responsive marketing website built with plain HTML, CSS, and JavaScript.

## Run locally

Start the local server:

```bash
npm run local
```

Then open:

- `http://127.0.0.1:8000`

## Alternative

You can also open `index.html` directly in your browser, or use:

```bash
python3 -m http.server 8000
```
